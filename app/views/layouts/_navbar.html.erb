<nav class="navbar navbar-inverse" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#tutor-navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Tutor Allocation" , root_url, class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="tutor-navbar-collapse">
      <ul class="nav navbar-nav">
        <li class="dropdown <%= "active" if active_controller?(controller_name, "tutor_applications") %>">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Tutor Applications <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
              <li><%= link_to "New Application", new_tutor_application_path %></li>
              <% if current_user && current_user.TM? %>
                <li><%= link_to "All Applications", tutor_applications_path %></li>
              <% end %>
          </ul>
        </li>
        <% if current_user && (current_user.TM? || current_user.CC?) %>
          <li class="<%= "active" if active?(request.path, courses_path)  %>" >
            <%= link_to "Courses", courses_path %>
          </li>
        <% end %>
        <% if current_user && current_user.TM? %>
          <li class="dropdown <%= "active" if (active_namespace?(controller, "Admin")) %>">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Admin <span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
                <li><%= link_to "General Configuration", admin_root_path %></li>
                <li><%= link_to "Hourly Rates", admin_hourly_rates_path %></li>
            </ul>
          </li>
        <% end %>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <li class="<%= "active" if active?(request.path, login_path)  %>">
          <%= link_to "Log in", login_path %>
        </li>
        <li class="<%= "active" if active?(request.path, signup_path)  %>">
          <%= link_to "Sign up", signup_path %>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
